This contains copies of other open source projects.

Google sparsehash is the exact same, except that I have deleted files I don't
need. The only thing I'm interested in is generating its config.h file.
